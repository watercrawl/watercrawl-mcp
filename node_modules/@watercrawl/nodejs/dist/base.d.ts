import { AxiosInstance, AxiosRequestConfig } from 'axios';
export declare class BaseAPIClient {
    protected client: AxiosInstance;
    protected baseUrl: string;
    protected apiKey: string;
    constructor(apiKey: string, baseUrl?: string);
    protected get<T>(path: string, params?: Record<string, any>): Promise<T>;
    protected post<T>(path: string, data?: Record<string, any>): Promise<T>;
    protected put<T>(path: string, data?: Record<string, any>): Promise<T>;
    protected delete<T>(path: string): Promise<T>;
    protected patch<T>(path: string, data?: Record<string, any>): Promise<T>;
    protected buildUrl(...parts: string[]): string;
    /**
     * Async generator that streams SSE data using fetch and yields parsed JSON
     */
    protected fetchStream<T>(endpoint: string, config?: AxiosRequestConfig): AsyncGenerator<T>;
}
